# Exclude regeneratable / platform-specific gradle-home subdirectories.
# These are re-created automatically when gradle runs; they are NOT needed
# for offline dependency resolution and would make the repo far too large.

# Gradle distribution binary (re-downloaded by gradlew on first use)
wrapper/

# JDK toolchain downloads (the system JDK is used instead)
jdks/

# Gradle 9.x internal transform / compilation caches (regenerated from the JARs)
caches/9.2.1/

# Daemon logs
daemon/

# Workers
workers/

# Native libs (regenerated)
native/

# Notifications
notifications/

# Lock files – must not be committed (would block concurrent gradle runs)
**/*.lock
